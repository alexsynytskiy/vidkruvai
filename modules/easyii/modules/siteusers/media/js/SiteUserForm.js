var SiteUserForm = function (options) {
    var pageOptions = $.extend(true, {
        mentorValue: ''
    }, options);

    var selectors = {
        optionItem: '.item'
    };

    var selection = {state: true};
};
